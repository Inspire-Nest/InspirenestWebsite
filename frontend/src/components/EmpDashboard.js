import React from "react";

const MyComponent = () => {
  // JavaScript code
  const message = "Hello from JavaScript in React!";

  return (
    <div>
      <h1 style={{ color: "black" }}>{message}</h1>
    </div>
  );
};

export default MyComponent;
